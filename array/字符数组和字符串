#include <stdio.h>
/*
main(){
	char ch[5]={'H','e','l','l','o'};   //单个字符赋值用‘’
//	char ch[5]="Hello";
//ch只分配了5个字节，'\0'被丢弃，所以L5这句话是不合法的字符串。“Hello”共5个字符，但内存占6个字节。
//L4和L5不等价，L4是合法的，不是字符串
//	char ch[6]={"Hello"};   //字符串常量赋值用“”
	printf("%s",ch);
}
*/
//数组名是字符数组/字符串在内存的首地址

main(){
	char str[5];   //输入串长度<数组维数
	scanf("%s",str);   //用字符数组命名，不要加&
	printf("%s",str);
}
