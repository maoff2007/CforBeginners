#include <stdio.h>

int f(int a, int b)
{
    int c;
    if (a > b) c = 1;
    else if (a == b) c = 0;
    else c = -1;
    return(c);
}

int main()
{
    int i = 2, p, q;
    //参数求值顺序从右向左
    p = f(i, ++i);   // f(3,3) >> 0
    q = f(i, i++);   // f(3,2) >> 1
    printf("p=%d, q=%d", p, q);
}
