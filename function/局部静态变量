#include <stdio.h>

int fac(int n) {
	static int f = 1;
	f = f * n;
	return(f);
}
//下一次调用时，f会保留上一次运算结束的值，从而起到求阶乘的作用

void main() {
	int i;
	for (i = 1; i <= 5; i++) {
		printf("%d!=%d\n", i, fac(i));
	}
}
